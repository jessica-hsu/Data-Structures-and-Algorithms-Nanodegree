function binary_search(number) {
    lo = -1;
    hi = number;

    while (lo + 1 < hi) {
        const distance = hi - lo;
        const half = Math.floor(distance/2);
        const guessIndex = floor + half;
    }
}